from turtle import Turtle, Screen

tim = Turtle()


for _ in range(4):

    tim.forward(20)
    tim.left(
        90
    )
    tim.forward(20)


for _ in range(5):
    tim.forward(70
    )
    tim.left(360/5)
    tim.forward(70

    )

for _ in range(7):

        tim.forward(80)
        tim.left(360/7)
        tim.forward(
            80
        )






wow = Screen()
wow.exitonclick()